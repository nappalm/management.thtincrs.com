import { ComponentStyleConfig } from '@chakra-ui/react';

// No funcionas las variantes, investigar e integrar
// en talent-groups y grupos en general
const avatar: ComponentStyleConfig = {
  variants: {
    group: {
      bg: '#134',
      borderRadius: 'sm',
    },
  },
};

export default avatar;
